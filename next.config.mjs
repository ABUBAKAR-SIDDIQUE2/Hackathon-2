// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // If you have a basePath or any other settings, add them here
    // basePath: '/my-base',

  };
  
  export default nextConfig;
  
